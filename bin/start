#!/usr/bin/env bash
# fl4re — App starten
# Verwendung: bin/start [PORT]

set -e

PORT=${1:-4000}

export DB_HOST=localhost
export DB_USER=microblog
export DB_PASSWORD=microblog_dev
export PORT=$PORT
export SOLID_QUEUE_IN_PUMA=1

echo "[fl4re] Starte auf Port $PORT ..."
exec bin/rails server
